import { keyOfColor } from '../../styles/theme';
interface PropsType {
    color: keyOfColor;
    onClick?: () => void;
}
export declare const SearchIcon: ({ color, onClick }: PropsType) => JSX.Element;
export {};
